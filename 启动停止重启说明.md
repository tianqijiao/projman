# 运维项目管理工具：启动、停止、重启说明

本文档适用于 Windows PowerShell。以下命令默认在“项目根目录”中执行。

## 1. 启动

先进入项目根目录，然后启动服务：

```powershell
.\scripts\start.ps1
```

启动成功后访问：

```text
http://127.0.0.1:8765
```

如果需要局域网内其他电脑访问，使用运行服务那台电脑的 IP：

```text
http://这台电脑的IP:8765
```

## 2. 停止

如果服务是在当前 PowerShell 窗口中启动的，直接在窗口里按：

```text
Ctrl + C
```

如果服务在后台运行，或找不到启动窗口，可以按端口停止：

```powershell
$listeners = Get-NetTCPConnection -LocalPort 8765 -State Listen -ErrorAction SilentlyContinue
foreach ($listener in $listeners) {
    Stop-Process -Id $listener.OwningProcess -Force
}
```

停止后，再访问 `http://127.0.0.1:8765` 应该打不开。

## 3. 重启

重启就是先停止，再启动。

```powershell
$listeners = Get-NetTCPConnection -LocalPort 8765 -State Listen -ErrorAction SilentlyContinue
foreach ($listener in $listeners) {
    Stop-Process -Id $listener.OwningProcess -Force
}

.\scripts\start.ps1
```

## 4. 后台启动

如果希望服务在后台启动，不占用当前 PowerShell 窗口，可以在项目根目录中使用：

```powershell
$scriptPath = Join-Path (Get-Location) "scripts\start.ps1"
Start-Process -WindowStyle Hidden -FilePath powershell -ArgumentList @(
    "-NoProfile",
    "-ExecutionPolicy",
    "Bypass",
    "-File",
    $scriptPath
)
```

后台启动后，仍然访问：

```text
http://127.0.0.1:8765
```

后台服务停止时，使用本文档第 2 节里的“按端口停止”命令。

## 5. 设置账号密码后启动

正式使用时，建议不要用默认密码。可以在启动前设置环境变量：

```powershell
$env:PROJMAN_USERNAME="admin"
$env:PROJMAN_PASSWORD="你的密码"
$env:PROJMAN_SECRET_KEY="一段随机字符串"

.\scripts\start.ps1
```

注意：上面的环境变量只对当前 PowerShell 窗口有效。关闭窗口后，下次需要重新设置。

也可以使用项目根目录下的 `.env` 文件保存长期配置。第一次配置时，把模板复制一份：

```powershell
Copy-Item .env.example .env
notepad .env
```

然后把 `.env` 里的密码和密钥改掉：

```text
PROJMAN_USERNAME=admin
PROJMAN_PASSWORD=你的密码
PROJMAN_SECRET_KEY=一段随机字符串
PROJMAN_DATABASE_URL=sqlite:///data/app.db
PROJMAN_DATA_DIR=data
```

以后直接运行：

```powershell
.\scripts\start.ps1
```

启动脚本会自动读取 `.env`。如果没有 `.env`，也能使用默认账号密码启动。

## 6. 迁移到另一台电脑

如果已经创建了 `.env`，迁移时建议一起复制。

如果没有 `.env`，另一台电脑也能启动，只是会使用默认账号密码。正式使用前建议在新电脑上按第 5 节创建 `.env`。

迁移时务必同时复制并保留：

```text
data/
.env
```

其中 `data/` 包含数据库和附件。

如果另一台电脑已经导入过附件，更新代码时应在原项目目录执行 `git pull`，不要删除或覆盖那台电脑上的 `data/` 和 `.env`。

## 7. 常见问题

如果提示端口 `8765` 已被占用，先执行停止命令，再重新启动。

如果局域网其他电脑打不开，优先检查：

- 服务是否已经启动。
- 访问地址是否写成了运行服务那台电脑的 IP。
- Windows 防火墙是否允许 Python 或端口 `8765` 入站。
- 两台电脑是否在同一局域网或同一个 Tailscale 网络。

## 8. 数据位置

项目数据保存在项目根目录下的 `data/`：

```text
data/app.db
data/attachments/
data/annual_attachments/
```

其中：

- `data/app.db` 是台账数据库。
- `data/attachments/` 是项目级 PDF 附件。
- `data/annual_attachments/` 是年度执行 PDF 附件。

迁移或备份时，务必保留整个 `data/` 目录。
